# 作业要求对应说明

| 检查项 | 本项目实现 |
| --- | --- |
| 使用传统视觉算法 | 是。使用灰度化、Otsu 阈值分割、最大连通区域、几何特征、投影特征和 KNN |
| 训练代码 | `train.m` |
| 独立测试代码 | `test.m` |
| 测试代码直接加载模型 | 是。加载 `gesture_model.mat`，不重新训练 |
| 批量读取 PNG 图像 | 是。读取测试目录下所有 `*.png` |
| 逐张输出识别结果 | 是。格式为 `文件名: 类别` |
| 输出类别合法 | 是。只输出 `A`、`C`、`Five`、`V` |
| 模型文件 | `gesture_model.mat` |
| 环境说明 | `ENVIRONMENT.md` 和 `startup_check.m` |
| 说明文档 | `README.md` |

最终提交文件包含训练、测试、预处理、特征提取、预测、模型、环境检查和说明文档，满足验收清单中的核心要求。
